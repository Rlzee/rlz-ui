---
title: Badge
description: Displays a badge or a component that looks like a badge.
---

<ComponentPreview name="badge" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTab value="cli">CLI</TabsTab>
  <TabsTab value="manual">Manual</TabsTab>
  <TabsIndicator />
</TabsList>

<TabsPanel value="cli">

```bash
npx rlz@latest add badge
```

</TabsPanel>

<TabsPanel value="manual">

<Steps>

<Step className="mb-4">Install the following dependencies:</Step>

```bash
npm install @base-ui/react
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="badge" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Usage

```tsx
import { Badge } from "@/components/ui/badge";
```

```tsx
<Badge>Default</Badge>
```

## API Reference

This is a custom component, using Base UI's `useRender` hook, not a direct Base UI wrapper.

### Badge

| Prop      | Type                                                                                   | Default     |
| --------- | -------------------------------------------------------------------------------------- | ----------- |
| `variant` | `"primary" \| "secondary" \| "outline" \| "error" \| "info" \| "success" \| "warning"` | `"default"` |
| `render`  | `React.ReactElement`                                                                   | -           |

## Examples

### Variants

Use the `variant` prop to change the appearance of the badge.

<ComponentPreview name="badge-variant" />
