---
title: Drawer
component: true
---

<ComponentPreview name="drawer" />

# Installation

```bash
npm: npx rlz@latest add drawer
pnpm: pnpm dlx rlz@latest add drawer
yarn: yarn rlz@latest add drawer
bun: bunx --bun rlz@latest add drawer
```

# Usage
```tsx
import { Drawer } from "@ui/components/drawer";
```

```tsx
export default function Example() {
  return (
    <Drawer>
      <Drawer.Trigger asChild>
        <button>Open</button>
      </Drawer.Trigger>
      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
          <Drawer.Description>Description</Drawer.Description>
        </Drawer.Header>
        <Drawer.Body>
          {/* content */}
        </Drawer.Body>
        <Drawer.Footer>
          <Drawer.Close>Close</Drawer.Close>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer>
  );
}
```

# Props

<Separator orientation="horizontal" />
<ComponentProps name="drawer" />

<MarkdownFallback>

## Drawer

The `Drawer` is a composed panel component exposing several slots:

- `main` — Root container.
- `Trigger` — Element that opens the drawer (supports `asChild`).
- `Portal` — Portal wrapper used for rendering content.
- `Overlay` — Backdrop overlay.
- `Content` — Drawer panel (supports `direction` and `overlay`).
- `Handle` — Small handle element (used for bottom/top drawers).
- `Header` / `Title` / `Description` — Header area.
- `Body` — Scrollable content area.
- `Footer` — Footer area for actions.
- `Close` — Close control.

Use the demo preview above to explore the component and its slots.

</MarkdownFallback>
