import { defineRegistry } from "@rlz/registry";

export const registryLibs = defineRegistry({
  cn: {
    name: "cn",
    type: "lib",
    description: "",
    path: "lib/cn.ts",
    version: "1.0.0",
  },
});
